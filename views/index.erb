<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
  <title>MemoApp | Top</title>
</head>
<body>
  <h1><a href="/memos" style="text-decoration:none; color:inherit;">✎メモアプリ</a></h1>
  <div> <button class="add-btn" onclick="location.href='/memos/new'">追加</button> </div>
  <div>
    <ul>
      <% if @memos == nil %>
        <%= @memos %>
      <% else %>
        <% @memos.each do |memo|  %>
          <% if memo.empty? %>
            <% nil %>
          <% else %>
            <li> <a href=<%="/memos/#{memo["memo_id"]}" %> ><%= memo["title"] %></a> </li>
          <% end %>
        <% end %>
      <% end %>
    </ul>
  </div>
</body>
</html>
